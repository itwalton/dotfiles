A collection of dotfiles to customize bash shell.

Installation
------------

- Clone repo with `git clone https://github.com/itwalton/dotfiles`

- Nav to installation directory and run `./install.sh`
